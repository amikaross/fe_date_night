<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">DATE NIGHT</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="nav flex-column">
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href=<%= user_dashboard_path %>>Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="#">Explore Nearby Places</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href=<%= appointments_path %>>Your Dates</a>
        </li>
        <li class="nav-item">
          <%= link_to "Logout", session_path(current_user), method: :delete, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Favorites", favorites_path, method: :delete, class: "nav-link" %>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div class="container">
  <h1>Restaurants Nearby You!</h1>

  <div id="search_places_form">
    <%= form_with url: places_path, method: :get, local: true do |f| %>
      <%= f.label :search_term, 'Keyword:' %><br>
      <%= f.text_field :search_term %><br><br>
      <%= f.submit 'Submit' %>
    <% end %>
  </div>

  <% @results.each do |place| %>
    <p><%= place.name %></p>
  <% end %>
</div>